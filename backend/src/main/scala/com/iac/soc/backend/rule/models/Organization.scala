package com.iac.soc.backend.rule.models

/**
  * Represents the organization of a rule
  *
  * @param id   the id of the organization
  * @param name the name of the organization
  */
case class Organization(id: Int, name: String)
