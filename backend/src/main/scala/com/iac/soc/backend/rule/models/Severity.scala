package com.iac.soc.backend.rule.models

/**
  * Represents the severity of a rule
  *
  * @param id   the id of the severity
  * @param name the name of the severity
  */
case class Severity(id: Int, name: String)
